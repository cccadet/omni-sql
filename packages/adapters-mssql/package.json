{
  "name": "@omni-sql/adapters-mssql",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --test --import ./src/index.test.ts",
    "coverage": "mkdir -p coverage && node --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-coverage-include='src/**/*.ts' --test-coverage-exclude='**/*.test.ts' --test --import ./src/index.test.ts"
  },
  "dependencies": {
    "@omni-sql/ts-types": "workspace:*",
    "@omni-sql/dialect-descriptors": "workspace:*",
    "@omni-sql/adapters-core": "workspace:*",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.5"
  }
}
