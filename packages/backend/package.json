{
  "name": "@omni-sql/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./protocol": "./src/protocol.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --test --import ./test/smoke.test.ts",
    "start": "node ./src/index.ts"
  },
  "dependencies": {
    "@omni-sql/ts-types": "workspace:*",
    "@omni-sql/dialect-descriptors": "workspace:*",
    "@omni-sql/adapters-core": "workspace:*",
    "@omni-sql/adapters-pg": "workspace:*",
    "@omni-sql/autocomplete-engine": "workspace:*",
    "@omni-sql/metadata-cache": "workspace:*"
  }
}