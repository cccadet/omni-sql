Production artifacts are staged here before `tauri build`:

- `backend/index.mjs`
- `runtime/node/node` (or `node.exe`)
- `runtime/jre/bin/java` (normalized on every platform)
- `sidecar/omni-sql-sidecar.jar`
