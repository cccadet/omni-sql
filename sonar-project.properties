sonar.projectKey=cccadet_omni-sql
sonar.organization=memory-mcp-server
sonar.projectName=omni-sql
sonar.sourceEncoding=UTF-8

# Analyze the maintained TypeScript/TSX and Rust trees only.
sonar.sources=apps/desktop/src,apps/desktop/src-tauri/src,packages
sonar.inclusions=**/*.ts,**/*.tsx,**/*.rs

# Dependencies, generated/build output, and local/infrastructure code are not
# application source and must not affect the SonarCloud project metrics.
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/coverage/**,**/.turbo/**,**/.cache/**,**/target/**,**/gen/**,**/*.rs.bk,docker/**,services/**
