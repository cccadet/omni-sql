#!/bin/sh
release=0
while read -r local_ref local_sha remote_ref remote_sha; do
  case "$local_ref" in
    refs/tags/v*) release=1 ;;
  esac
done
pnpm test:coverage || exit $?
if [ "$release" -eq 1 ]; then
  exec bash "$(git rev-parse --show-toplevel)/scripts/pre-release.sh"
fi
