# omni-sql v0.2.3

omni-sql is an open-source desktop SQL IDE for developers who work across multiple database engines. It combines schema exploration, a Monaco-powered SQL editor, contextual autocomplete, query results, guarded row editing, and execution plans in one local workspace.

v0.2.3 makes table maintenance and connection setup easier to understand and safer to review. Structure and index editors now scale better to real schemas, provide context-aware suggestions, and keep generated SQL visible before anything is executed.

## New in v0.2.3

- **Improved table structure editor** — edit column names, types, nullability, primary keys, and default values in a denser, scrollable layout with persistent actions.
- **Dialect-aware defaults** — default-value suggestions now reflect the active PostgreSQL, MySQL, MariaDB, SQL Server, Oracle, or JDBC dialect.
- **Faster column creation** — new columns receive suggested names, types, and defaults, while focus remains in the name field for immediate editing.
- **Redesigned index editing** — indexes use structured cards, ordered column chips, search-based column selection, and explicit controls for reordering or removing columns.
- **Better large-table behavior** — index column choices stay compact and searchable instead of expanding into an unwieldy list.
- **Clearer schema selection** — connection setup now includes schema search, alphabetical ordering, selected schemas first, selection counts, visible-item selection, and a clearer all-schemas mode.
- **More consistent dialogs** — connection editing now uses balanced width, aligned fields, compact spacing, bounded scrolling, and padded titles and actions.
- **English and Brazilian Portuguese coverage** — the new structure, index, default, and schema-selection guidance is localized in both interface languages.

## Review-first database changes

Table and index changes continue to open as generated SQL in a new editor tab. You can inspect and adjust the statements before choosing whether to execute them against a database.

## Supported databases

- PostgreSQL
- MySQL
- MariaDB
- SQL Server
- Oracle
- Generic JDBC (experimental, with limited metadata and execution features)

## Downloads

- Windows 10/11 x64: `.exe` installer
- Debian/Ubuntu amd64: `.deb` package
- `SHA256SUMS`: checksums for the published installers

The installers bundle the application runtimes. End users do not need to install Node.js, Java, Rust, a separate database client, or a vendor client SDK.

## Before you start

omni-sql is early-stage software. Try it with a development database before using it against important environments, and report unexpected behavior through GitHub Issues.

v0.2.3 changelog: [`v0.2.2...v0.2.3`](https://github.com/cccadet/omni-sql/compare/v0.2.2...v0.2.3)

Complete v0.2 series: [`v0.1.39...v0.2.3`](https://github.com/cccadet/omni-sql/compare/v0.1.39...v0.2.3)

Documentation: [github.com/cccadet/omni-sql](https://github.com/cccadet/omni-sql#readme)
